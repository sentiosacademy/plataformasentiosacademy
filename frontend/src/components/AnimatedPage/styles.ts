import { motion } from 'framer-motion';
import styled from 'styled-components';

// Usamos styled(motion.div) para criar um div que aceita as props de animação
export const AnimatedWrapper = styled(motion.div)`
  width: 100%;
`;