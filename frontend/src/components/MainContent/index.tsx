import styled from 'styled-components';

export const MainContent = styled.div`
  /* A regra que faz o layout do "sticky footer" funcionar,
     forçando este container a ocupar todo o espaço vago. */
  flex: 1;
`;